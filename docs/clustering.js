export function cluster_seqs(maximal_seqs) {
    /*
        Given a list of maximal sequences, return a list of lists of clusters.

        [ [cluster 1], [cluster 2]], with similar sequences grouped together.
    */
}